<?xml version="1.0" encoding="utf-8"?>
<!-- Logotipo de marca compartido con la app de escritorio (JavaFX): un icono de
     "pulso" (línea de electrocardiograma). Mismo path SVG que el escritorio para
     que ambos clientes se reconozcan como el mismo producto clínico.
     Se dibuja como TRAZO (no relleno): es una línea, no una silueta. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="48dp"
    android:height="48dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/color_primary">
    <path
        android:pathData="M22,12h-6l-2,5 -4,-10 -2,5H2"
        android:strokeColor="@android:color/white"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
